import {TILE} from '../constants'

export const PlayerUI = {
    name: 'Player 1',
}

export const PlayerEntity = {
    type: 'box',
    width: TILE,
    height: TILE,
    x: 0,
    y: 0,
}

export default PlayerEntity
